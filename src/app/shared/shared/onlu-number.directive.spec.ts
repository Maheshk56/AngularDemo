import { OnluNumberDirective } from './onlu-number.directive';

describe('OnluNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new OnluNumberDirective();
    expect(directive).toBeTruthy();
  });
});
